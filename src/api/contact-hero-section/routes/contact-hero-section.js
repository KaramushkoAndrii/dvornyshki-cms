'use strict';

/**
 * contact-hero-section router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::contact-hero-section.contact-hero-section');
